# GREVEN — environment variables
# Copy to `.env.local` and fill in. Never commit real values.

# Public site URL, used for metadata / Open Graph / sitemap.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Contact form endpoint (optional; a mailto fallback is used when unset).
# NEXT_PUBLIC_CONTACT_ENDPOINT=
